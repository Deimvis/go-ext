# xcheck

xcheck — a collection of Golang packages with different kinds of validations.

NB: `xcheck` one of not so many modules that is allowed to be used from other ext modules and packages.
TODO: separate such modules somehow to make it more evident.

TODO: custom packages for complex types or type specific checks (e.g. xshouldjson.Eq, xmustjson.Eq, xshouldstr.NotEmpty, ...)
